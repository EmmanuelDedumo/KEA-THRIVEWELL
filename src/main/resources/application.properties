# Application Metadata
spring.application.name=ThriveWell
server.port=${PORT:8080}  # Use Railway's PORT variable, fallback to 8080 for local

# Database Configuration (for Railway)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:dbthrivewellbackend}?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Iloveomori.2}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# HikariCP Connection Pool Settings (optional, recommended for production)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JWT Configuration
security.jwt.secret-key=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
security.jwt.expiration-time=3600000  # 1 hour in milliseconds

# Google OAuth Configuration
google.client.id=523582901522-lopkjavvunrcv7ocr7mg6q5376gtb4g4.apps.googleusercontent.com

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thrivewell.noreply@gmail.com
spring.mail.password=${MAIL_PASSWORD:yune njvh kjjb acyl}  # Use environment variable for security
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000

# Application-Specific Properties
app.name=ThriveWell
app.frontend-url=${FRONTEND_URL:http://localhost:3000}  # Use environment variable for Railway
app.password-reset-token-expiry=3600  # 1 hour in seconds